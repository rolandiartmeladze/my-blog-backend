[phases.setup]
nixPkgs = ["php", "composer"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "php artisan config:cache",
  "php artisan migrate --force" # თუ გინდა რომ ავტომატურად ბაზა შეიქმნას
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8080"
